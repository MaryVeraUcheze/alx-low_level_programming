#include <stdio.h>
/**
 * main - Entry point
 * Description: prints the last digit of a random number
 * stored in the variable n
 * Return: Always 0 (Success)
 */
int main(void)
{
char letter = 'a';

while (letter <= 'z')
{
	putchar(letter);
	letter++;
}
putchar('\n');
return (0);
}
